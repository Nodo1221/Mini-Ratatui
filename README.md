# mini-ratatui

A minimal showcase of modern [ratatui](https://ratatui.rs/) (0.30) covering the core concepts: layouts, widgets (stateless and stateful), styling, event handling, all in a single interactive annotated demo. Use for reference.

### Run with

```bash
cargo run
```